============
Installation
============

At the command line::

    $ pip install keystone_utils

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv keystone_utils
    $ pip install keystone_utils
